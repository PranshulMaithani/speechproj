torch>=2.0
transformers>=4.40
librosa>=0.10
soundfile>=0.12
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
tqdm>=4.65
xgboost>=2.0             # xgboost_train.py (tier A/B fusion variants)
audiomentations>=0.33    # extract_embeddings.py augmentations
huggingface_hub>=0.20    # allstar_prep.py (download)
faster-whisper>=1.0      # allstar_prep.py (transcription with word timestamps)
spacy>=3.7               # full_text_features.py POS features. After install:
                         #   python -m spacy download en_core_web_sm
praat-parselmouth>=0.4   # full_text_features.py voice quality (jitter/shimmer/HNR)
